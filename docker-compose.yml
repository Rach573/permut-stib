services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: permutstib
      POSTGRES_USER: permutstib
      POSTGRES_PASSWORD: local-dev-only
    ports:
      - "5432:5432"
    volumes:
      - permutstib_pgdata:/var/lib/postgresql/data

volumes:
  permutstib_pgdata:

